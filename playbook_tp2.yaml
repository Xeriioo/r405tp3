---
- name: Update apt source
  hosts: all
  become: true


  tasks:
    - name: Update apt souce
      ansible.builtin.apt:
        update_cache: true

- name: Install apache on web
  hosts: web

  tasks:
    - name: Install apache2
      ansible.builtin.apt:
        name: apache2
        state: present
